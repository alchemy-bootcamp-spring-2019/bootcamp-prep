// Arrays
  
const arrayOfNums = [1, 2, 3];

arrayOfNums;

arrayOfNums[0];

arrayOfNums[1];

arrayOfNums[2];

arrayOfNums.push(4);

arrayOfNums;

const arrayOfDogs = ['fido', 'spot', 'lassie', 'doug'];

arrayOfDogs.pop();

arrayOfDogs;

arrayOfDogs.push('doug');

arrayOfDogs;

arrayOfDogs.pop();

arrayOfDogs;

arrayOfDogs.pop();

arrayOfDogs;

// Objects

  const objectGreeting = {
    hi: 'hello',
    bye: 'goodbye'
  }

  objectGreeting;

  objectGreeting.hi;

  objectGreeting.bye;

  objectGreeting.celebrate = 'high five!';

  objectGreeting.celebrate;

  objectGreeting;

  Object.keys(objectGreeting);

  const myDog = {
    name: 'max',
    weight: '5 lbs',
    breed: 'pug'
  }

  myDog;

  myDog.name;

  myDog.weight;

  myDog.breed;

  Object.keys(myDog);

  myDog;

  myDog.favoriteFood = 'pizza';

  myDog.favoriteFood;

  myDog;
